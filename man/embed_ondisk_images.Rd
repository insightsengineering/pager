% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed_images.R
\name{embed_ondisk_images}
\alias{embed_ondisk_images}
\title{Embed On-Disk Images Into a Word Document (internal)}
\usage{
embed_ondisk_images(path)
}
\arguments{
\item{path}{(\code{string})\cr
path to the \code{.docx} file to repair.}
}
\value{
\code{path}, invisibly. Called for the side effect of rewriting the file.
}
\description{
Repairs a \code{.docx} file whose images are referenced by absolute file-system
paths rather than packaged into the archive.

When a \code{flextable} containing inline images (for example, the forest-plot
column produced by \code{crane::add_forest()}) is rendered to Word through the
R Markdown/pandoc path, the resulting \verb{<w:drawing>} elements reference the
images by their temporary on-disk path (\code{r:embed="/tmp/.../file.png"}). Those
files are never copied into the archive, so Word cannot open the document.

This function copies each referenced image into \verb{word/media/}, registers an
image relationship for it, rewrites the \code{r:embed} attribute to the new
relationship id, and ensures the image's content type is declared. The
archive is repacked in place.
}
\keyword{internal}
