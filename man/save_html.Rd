% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_as_html.R
\name{save_html}
\alias{save_html}
\title{Save as HTML}
\usage{
save_html(x, path, css = NULL)
}
\arguments{
\item{x}{(\code{gtsummary}/\code{gt_tbl}/\code{flextable}/\code{ggplot}/\code{grob}/\code{list})\cr
object of class \code{'gtsummary'}, \code{'gt_tbl'} (gt table), \code{'flextable'}, \code{'ggplot'},
\code{'grob'}, or a list of these objects.}

\item{path}{(\code{path})\cr
path to save file to, e.g. \code{"rendered_table.html"}.}

\item{css}{(\code{string})\cr
optional CSS style block to embed in the HTML output. Default is \code{NULL}
(no custom CSS). Example: \code{"body { font-family: Arial; font-size: 12px; }"}.}
}
\value{
(invisibly) a \code{string} corresponding to the content of the intermediate
\code{.rmd} file that is rendered as \code{.html}.
}
\description{
Save input in a self-contained \code{.html} file via R markdown.
}
\examples{
# create table
tbl <-
  cards::ADAE[1:150, ] |>
  gtsummary::tbl_hierarchical(
    variables = c(AESOC, AETERM),
    by = TRTA,
    denominator = cards::ADSL,
    id = USUBJID,
  )

# save a gtsummary table as html
tbl |>
  save_html(path = tempfile(fileext = ".html"))

# save as html with gt
tbl |>
  gtsummary::as_gt() |>
  save_html(path = tempfile(fileext = ".html"))

# save as html with custom CSS
tbl |>
  gtsummary::as_gt() |>
  save_html(
    path = tempfile(fileext = ".html"),
    css = "body { font-family: Arial; font-size: 12px; }"
  )

# save a paginated table as html — pages are separated by a horizontal rule
gtsummary::tbl_split_by_rows(tbl, row_numbers = seq(20, nrow(tbl), by = 20)) |>
  save_html(path = tempfile(fileext = ".html"))

}
