# get_reference_docx() aborts when no reference document matches

    Code
      get_reference_docx(font_size = 99L)
    Condition
      Error in `get_reference_docx()`:
      ! No reference Word document selected.
      i Run `get_all_reference_docx()` to print possible selections.

